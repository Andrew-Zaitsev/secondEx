mixin pagination(options)
  .pagination
    - 
      let roomCardsClassName = "pagination__room-cards";
      roomCardsClassName += (options.areCardsHidden) ? " pagination__room-cards_hidden" : "";

    div(class = roomCardsClassName)
      //- сделать блок room-card
    .pagination__buttons